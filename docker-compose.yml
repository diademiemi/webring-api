services:
  postgresql:
    image: bitnami/postgresql:latest
    ports:
      - 5432:5432
    volumes:
        - webring_data:/bitnami/postgresql
    environment:
      - POSTGRESQL_USERNAME=webring
      - POSTGRESQL_PASSWORD=webring
      - POSTGRESQL_DATABASE=webring

volumes:
  webring_data:
    driver: local
